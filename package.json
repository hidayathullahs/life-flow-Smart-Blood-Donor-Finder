{
  "name": "smartbloodlife-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm install --prefix web && npm run build --prefix web && node -e \"require('fs').cpSync('web/dist', 'dist', {recursive: true})\""
  }
}
